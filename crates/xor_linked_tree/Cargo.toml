[package]
name = "xor_linked_tree"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

[dependencies]

[dev-dependencies]
bench = { path = "../bench" }
criterion = "0.8.1"
rand = "0.9"

[[bench]]
name = "xor_linked_tree"
harness = false

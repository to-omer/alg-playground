[package]
name = "ordered_map"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

[dependencies]

[dev-dependencies]
bench = { path = "../bench" }
criterion = "0.8.1"
rand = "0.9"

[[bench]]
name = "ordered_map"
harness = false
path = "benches/ordered_map.rs"


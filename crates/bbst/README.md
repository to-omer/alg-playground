# bbst

Balanced binary search tree experiments with a shared sequence API.
This crate currently provides a sample core abstraction, an implicit treap,
and benchmark harnesses for workload/feature combinations.

## Status
- Core traits and policies: implemented
- Implicit treap: implemented (range sum + range add)
- Benchmarks: workload x feature sets
